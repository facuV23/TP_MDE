#include <stdint.h>
#include <stdio.h>
#include <string>

namespace respuesta{

void hola(string resp);
void error(string resp);
void ok(string resp);
bool reconocer(string resp);
bool pass(string resp);
void init(string resp);

}