#include <stdint.h>
 namespace tecla {

  int init(uint8_t tecla);
  int8_t suelto(uint8_t tecla);
  int8_t presiono(uint8_t tecla);
  int8_t ventana_asce(uint8_t tecla);
  int8_t ventana_desc(uint8_t tecla);


 } 